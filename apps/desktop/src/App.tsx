import "./App.css";

function App() {
  return (
    <main className="container">
      <h1>Computa</h1>
      <p className="tagline">
        Local-first dictation for macOS — global hotkey, transcribe, inject at
        the cursor.
      </p>
      <p className="status">Early scaffold. The dictation pipeline is not wired up yet.</p>
    </main>
  );
}

export default App;
